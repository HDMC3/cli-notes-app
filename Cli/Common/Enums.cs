namespace Cli.Common {
    public enum MainMenuOptions {
        ShowNotebooks,
        CreateNotebook,
        EditNotebook,
        CreateNote,
        SearchNote,
        Exit
    }

    public enum ListOptions {
        Item,
        Back
    }

    public enum  NotebookOptions {
        ShowNotes,
        EditNote,
        DeleteNote,
        BackNotebookList
    }
}